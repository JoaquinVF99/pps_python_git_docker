import random

def frotar(n_frases: int = 1) -> list:

     frase_auspiciosa = "Que la suerte esté siempre de tu lado."  # Frase única
     return [frase_auspiciosa] * n_frases  # Repetir la misma frase N veces

